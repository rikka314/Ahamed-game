# S0 基线验证报告

> 执行日期：2026-08-29  
> 工作区：`D:\Learn\20_Projects\MedicalAI\apps\game`

## 结论

S0 自动基线通过。game 与 share 均可在本机 Node 24.18.0 / npm 11.17.0 下完成各自质量门；game 的 Node engine 已从 `>=20.9` 对齐为 `>=22.18`。H0/H1 的开放产品项已有可执行的临时默认，但仍等待项目负责人确认。

## 起始状态

- Git：`main...origin/main [ahead 2]`，工作树在任务开始前已有 `AI_CONTEXT.md`、baseknowledge、model、share 和 `docx/plan` 等用户改动；本轮没有回退、覆盖或清理这些无关改动。
- game：Next.js 16.3.2、React 19.2.8、Phaser 4.2.1；起始实现为程序矩形诊所 PoC。
- share：包版本 `1.0.0-rc.1`，公共 `contractVersion=1` / release `v1-rc1`；game adapter 仍未实现。
- 内容版本：`contentBuildId=dev`。
- 尚未存在：`saveSchemaVersion`、`rewardRuleVersion`、真实模型 HTTP/SSE adapter、持久恢复、诊疗/评价、经营系统和正式美术。

## 基线命令

| 范围 | 命令 | 结果 |
|---|---|---|
| game | `npm run lint` | 通过 |
| game | `npm run typecheck` | 通过 |
| game | `npm test` | 通过：2 个测试文件，6 个测试 |
| game | `npm run build` | 通过：Next 生产构建成功 |
| share | `npm run typecheck` | 通过 |
| share | `npm test` | 通过：85 个测试 |
| share | `npm run test:contract` | 通过：80 个 contract 测试 |

以上为 S0 起始基线；S2 新增后的最终结果记录在 `S2-工程验收报告.md`。

## 边界与安全盘点

- 仓库扫描未发现需要加入 game 的真实 API Key；浏览器端新增代码不读取模型密钥。
- `game/public` 只发布 `contentBuildId=dev` 的 Tiled placeholder 地图和 manifest。
- model 的 draft、病例答案、隐藏事实、rubric、prompt 与评估证据均未复制到 game/public。
- S2 使用公开合成显示名与流程 ID，不声称这些患者是已批准病例。
- 真实 Integrated MVP 仍受 share adapter、可信 transport、模型 H8 与后续 S10a/S10b 门控。

## 可重复入口

```powershell
cd D:\Learn\20_Projects\MedicalAI\apps\game\game
npm run dev
# http://localhost:3020
```

决策与待确认项见 `decisions/S0-决策日志与人工门.md`。

