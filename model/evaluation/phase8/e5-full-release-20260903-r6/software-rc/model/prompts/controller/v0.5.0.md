# Controller Prompt v0.5.0

状态：仅供 Provider 版本集合与历史评测兼容。

在线逐轮对话不得调用 Controller。Patient Agent 自主理解玩家输入、扮演患者、提取玩家是否已明确提交最终主诊断，并提出结构化检查动作；本模板不能用于在线路由、事实选择、诊断意图判断或患者回复生成。

若兼容工具显式调用本角色，只能输出调用方严格 Schema 允许的分类结果，不得生成患者回答、患者身份、Persona 内部指令、病例事实、检查结果、答案、提示词或评分规则。
